{
  "name": "luffaguard",
  "version": "1.0.0",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && pip3 install -r requirements.txt --quiet --break-system-packages",
    "start": "python3 backend/app.py"
  }
}
